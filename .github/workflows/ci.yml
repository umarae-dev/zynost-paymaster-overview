name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  paymaster:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - name: Public repository guard
        run: node scripts/check-public-repo.js
      - name: Install
        run: npm install
      - name: Compile
        run: npm run compile
      - name: Production paymaster tests
        run: npm test
